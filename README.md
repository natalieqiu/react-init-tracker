# react-init-tracker
I developed this by myself which is why it is questionable

### to deploy
``npm run deploy`` and your password is a personal access token.

note to self: fine-grained token should have the following `r&w` perms
* contents 
* actions
* pages

and then save it as an env variable
``export GH_TOKEN=your_personal_access_token``
# future goals?
room code generation (i believe this requires a backend)

